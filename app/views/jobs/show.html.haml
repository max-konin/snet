%p#notice= notice
.row
  .col-md-8
    #map
  .col-md-4
    - @job.regions.each do r
      = render 'regions/region', locale: { region: r }
    %button.add_region
      Add

\|
= link_to 'Back', jobs_path
= javascript_include_tag  "jobs", "data-turbolinks-track" => false
